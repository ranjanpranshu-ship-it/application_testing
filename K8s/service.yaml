apiVersion: v1
kind: Service
metadata:
  name: application-testing-api-service
spec:
  type: NodePort
  selector:
    app: application-testing-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30001  # Optional: specific port to expose   